/**
 * Feature Modules Package
 *
 * Reserved for pluggable feature modules. Currently a placeholder —
 * module metadata lives in the database (Module / GuildModule models)
 * and command categories map to module names.
 */

export {};
